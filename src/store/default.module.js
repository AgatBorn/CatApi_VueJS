const initialState = {
};

export const state = { ...initialState };

export const actions = {
};

export const mutations = {
};

export const getters = {
};

export default {
  state,
  actions,
  mutations,
  getters,
};
